{
  "name": "grafana-mcp-adapter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only Grafana MCP adapter (dashboards + datasources + queries) for local-tooling",
  "scripts": {
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check server.js grafanaClient.js helpers.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.5",
    "zod": "^3.24.1"
  },
  "engines": {
    "node": ">=18"
  }
}
